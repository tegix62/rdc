node_modules/
dist/
.astro/
.env
.env.*
!.env.example
.DS_Store

# package-lock.json was ignored here, which meant every CI run resolved
# dependency versions afresh from the ^ ranges in package.json. Two deploys
# broke that way earlier in this project. It is committed now: the versions that
# built the site today are the versions that build it on launch day.

# Generated by scripts/build-overlay.mjs for preview builds only.
public/sanity-visual-editing.js
