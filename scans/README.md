# scans

Raw artwork drops for the charcoal → web pipeline.

Put originals here at full resolution, unretouched:

- `darkdominion-overlay-charcoal.jpg` — white charcoal on black paper, the raw scan
- anything else to be run through `charcoal.py`

Nothing in this folder ships. It is source art; the build emits masks from it.
