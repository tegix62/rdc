# Video upload endpoint (Cloudflare Pages Function → R2)
# Set these so the Studio's drag-and-drop video uploader works.
# Without them, the field falls back to a plain URL paste.
SANITY_STUDIO_UPLOAD_URL=https://rumeaudesign.co/api/upload-video
SANITY_STUDIO_UPLOAD_TOKEN=your-upload-token-here
